import React from "react"
import { observer } from "mobx-react"
// import { store } from "../store"

const Header = () => {
  return <div className="p-2 bg-white h-16 flex-none">header</div>
}

export default observer(Header)
